# Salpakan
A multiplayer board game

## How to run

### Pre-requisites

- Node 14 or higher
- Postgresql server

#### API

The API is ...
We need to initialize the postgresql with database and tables before can do anything. 

To create the database run the command below in your postgres terminal.
```
insert here 
```
After we've created the database, let's create the two tables - accounts and game - . To create the account table run the the following command below in your postgres terminal. 

```
```

We will run a similar command to create the games table. 

#### Client

The client is the front-ent UI ...
